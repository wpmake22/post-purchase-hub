<?php
/**
 * A theme's own orders template, as a fixture.
 *
 * Stands in for the copy a commercial theme ships. Its contents are irrelevant:
 * the conflict scanner looks for the file, not at it.
 *
 * @package PostPurchaseHub
 */

declare( strict_types = 1 );

defined( 'ABSPATH' ) || exit;
