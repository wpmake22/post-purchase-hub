/**
 * Pro frontend entry point.
 *
 * Intentionally empty. It exists so pro/assets/build is a real directory before
 * any Pro feature needs it — bin/build.php refuses to build the Pro zip when
 * pro/assets/src exists without a matching build.
 */
